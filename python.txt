url:
https://conda.io/miniconda.html

run in anaconda prompt:
conda update conda
conda update python
conda install ipython requests lxml beautifulsoup4 sqlite pyqt numpy pandas matplotlib 
conda install statsmodels scipy scikit-learn nltk networkx cython
conda install spyder jupyter quandl 
conda install -c conda-forge pyinstaller
conda install pip
pip install -U pip
pip install pulp
conda clean -t
conda clean -p

update packages:
python -m pip install --upgrade pip
pip install package_name --upgrade
conda update --all
pip freeze --local | grep -v '^\-e' | cut -d = -f 1  | xargs -n1 pip install -U

set path:
in path variable ->
\Miniconda3
\Miniconda3\Scripts
\Miniconda3\Lib\site-packages

install gurobi (optimization package):
conda config --add channels http://conda.anaconda.org/gurobi
conda install gurobi

optimization:
conda install -c cvxgrp cvxpy
conda install -c mosek mosek 